---
name: dependency-tracker
description: Find and update all references after renames, moves, config changes. Triggers: "check dependencies", "update references", "what references X", "syn...
user-invocable: true
---
<dependency-tracker>
Call the skill-loader MCP tool `load_skill` with name "dependency-tracker" to get full instructions, then follow them.
</dependency-tracker>
